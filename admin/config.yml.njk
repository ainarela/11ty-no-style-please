---
# generate the yml using 11ty, so we can configure the CMS, from the CMS! idk if it is a good idea...
permalink: admin/config.yml
---

backend:
  name: git-gateway
  branch: main

media_folder: media/uploads # Media files will be stored in the repo under media/uploads
public_folder: /media/uploads # The src attribute for uploaded media will begin with /media/uploads

{% if site.url %}
site_url: {{ site.url }}
{% endif %}

collections:
  - label: "📝 Notes"
    name: notes
    folder: posts/notes
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", "note"] }
      - { label: Content, name: body, widget: markdown }
  - label: "🏞 Images"
    name: images
    folder: posts/images
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", "image"] }
      - { label: Image, name: image, widget: image }
      - { label: Caption, name: body, widget: markdown, minimal: true, required: false }
  - label: "🎥 Videos"
    name: videos
    folder: posts/videos
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", "video"] }
      - { label: Type, name: type, widget: string, required: false }
      - { label: File, name: file, widget: file }
      - { label: Caption, name: body, widget: markdown, minimal: true, required: false }
  - label: "🎙 Audios"
    name: audios
    folder: posts/audios
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", "audio"] }
      - { label: Type, name: type, widget: string, required: false }
      - { label: File, name: file, widget: file }
      - { label: Caption, name: body, widget: markdown, minimal: true, required: false }
  - label: "🧩 Embedded"
    name: embedded
    folder: posts/embedded
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", "embedded"] }
      - { label: "Embedded code", name: code, widget: markdown }
      - { label: Caption, name: body, widget: markdown, minimal: true, required: false }
  - label: "🔖 Quotes"
    name: quotes
    folder: posts/quotes
    create: true
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: YYYY-MM-DD }
      - { label: Tags, name: tags, widget: list, default: ["post", quote] }
      - { label: Source, name: source, minimal: true, widget: markdown }
      - { label: Quote, name: body, widget: markdown }
